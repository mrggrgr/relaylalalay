# Tiny project with GraphQL & Relay

## Installation

```
npm install
```

## Running

Set up relay generated files:

```
npm run relay
```

Start a local server:

```
npm run start
```

Open `http://localhost:3001/` at browser.